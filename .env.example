# Backend OpenAI-compatible endpoint
BACKEND_URL=https://llm.chutes.ai/v1/chat/completions

# REQUIRED: Your backend API key (Chutes, OpenAI, etc.)
# This key is used for ALL backend requests, regardless of what Claude Code sends
BACKEND_KEY=your-chutes-api-key-here

# Model to use for test requests
MODEL=zai-org/GLM-4.5-Air

# Optional: API key for test scripts only (not used by proxy)
API_KEY=your-test-api-key

# Logging level: error, warn, info (default), debug, trace
RUST_LOG=info
